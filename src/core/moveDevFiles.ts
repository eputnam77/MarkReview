/** Move project management files into the dev/ directory.
 *
 * Actual file system operations are not implemented yet; this stub simply
 * returns a list of files that would be moved.
 */
export function moveProjectManagementFiles(): string[] {
  return ['AGENTS.md', 'TASKS.md', 'ROADMAP.md', 'NOTES.md']
}
