import { describe, it, expect } from 'vitest'
import * as mod from '../src/core/comments.ts'

describe('comments', () => {
  it('needs implementation', () => {
    expect(mod).toBeDefined()
    expect.fail('TODO: implement comments tests')
  })
})
