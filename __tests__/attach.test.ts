import { describe, it, expect } from 'vitest'
import * as mod from '../src/index.ts'

describe('attach', () => {
  it('needs implementation', () => {
    expect(mod).toBeDefined()
    expect.fail('TODO: implement attach tests')
  })
})
