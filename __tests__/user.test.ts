import { describe, it, expect } from 'vitest'
import * as mod from '../src/api/user.ts'

describe('user', () => {
  it('needs implementation', () => {
    expect(mod).toBeDefined()
    expect.fail('TODO: implement user tests')
  })
})
